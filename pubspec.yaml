name: smart_library
description: "Technology Book Library with Smart Search"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.4.4
  google_sign_in: ^6.1.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  http: ^1.1.0

flutter:
  uses-material-design: true
